# Run the below commands in the Cloud Shell

```bash
export Region=
```

```bash
export Zone=
```

# Task 1: A new non-default VPC has been created

```bash
gcloud compute networks create securenetwork --subnet-mode custom
```
